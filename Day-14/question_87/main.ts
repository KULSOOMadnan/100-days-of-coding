function limitation (str : string) {
    // SUBstring is a method which takes 2 parameter starting and ending
    // it will write the specifying index in string
    return str.substring(0 ,15)
}

console.log(limitation("Hello everyone! hope you are doing well, fit and fine"));
