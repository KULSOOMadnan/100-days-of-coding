
interface Car {
    model: number,
    name : string,
    no_plate : string
}

let car : Car = {
    model : 2024,
    name : "fortuner",
    no_plate :"KAS 777"
}

console.log(car);
console.log(car.model);
console.log(car.name);
